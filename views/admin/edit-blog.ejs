<!DOCTYPE html>
<html>
<head>
    <title>Edit Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-5">
    <h2 class="mb-4">Edit Blog</h2>

<form method="POST"
      action="/admin/update_blog/<%= blog.blog_id %>"
      enctype="multipart/form-data">

    <div class="mb-3">
        <label>Blog Title</label>
        <input type="text"
               name="blog_title"
               class="form-control"
               value="<%= blog.blog_title %>"
               required>
    </div>

    <div class="mb-3">
        <label>Short Description</label>
        <textarea name="blog_short_description"
                  class="form-control"
                  rows="2"><%= blog.blog_short_description %></textarea>
    </div>

    <div class="mb-3">
        <label>Full Description</label>
        <textarea name="blog_full_description"
                  class="form-control"
                  rows="5"><%= blog.blog_full_description %></textarea>
    </div>

    <div class="mb-3">
        <label>Blog Image</label><br>

        <% if(blog.blog_image){ %>
        <img src="/assets/images/<%= blog.blog_image %>"
             width="140"
             class="mb-2 rounded border">
        <% } %>

        <input type="file"
               name="blog_image"
               class="form-control">
    </div>

    <div class="mb-3">
        <label>Publish Date</label>
        <input type="date"
               name="blog_publish_date"
               class="form-control"
               value="<%= publishDate %>">
    </div>

    <button class="btn btn-success">Update Blog</button>
    <a href="/admin/blog" class="btn btn-secondary ms-2">Cancel</a>

</form>

</div>

</body>
</html>
